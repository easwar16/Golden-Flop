# ─── Server ───────────────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=development

# ─── Database ─────────────────────────────────────────────────────────────────
# PostgreSQL connection string (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://goldenflop:goldenflop_dev@localhost:5432/goldenflop?schema=public"

# Docker Postgres config (only used by docker-compose.yml)
POSTGRES_USER="goldenflop"
POSTGRES_PASSWORD="goldenflop_dev"
POSTGRES_DB="goldenflop"

# ─── Redis ────────────────────────────────────────────────────────────────────
# Leave blank to run without Redis (ephemeral state only)
REDIS_URL="redis://localhost:6379"

# ─── JWT ──────────────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="change_me_to_a_secure_random_256_bit_secret"
JWT_EXPIRES_IN="30d"

# ─── Solana ───────────────────────────────────────────────────────────────────
# "devnet" | "mainnet-beta"
SOLANA_NETWORK="devnet"

# Treasury wallet that receives all deposits (rake destination)
# This is a PUBLIC KEY only
TREASURY_WALLET="YourTreasuryPublicKeyBase58Here"

# Seeker SPL token mint address
SEEKER_MINT="SeekerTokenMintAddressBase58Here"

# ─── Vault (escrow) ─────────────────────────────────────────────────────────
# Shared vault private key (base58-encoded 64-byte secret key) — used for all
# rooms unless a per-room override is set. Leave blank to disable vault mode.
VAULT_PRIVATE_KEY=""

# Per-room vault key overrides (production). Format: VAULT_KEY_<ROOM_ID_UPPERCASED>
# e.g. VAULT_KEY_TABLE_LOW_1="base58SecretKeyHere"

# ─── CORS ─────────────────────────────────────────────────────────────────────
CORS_ORIGIN="*"
